'use client'

import React from 'react'
import { ConfigProvider } from 'antd'

const HomePage = () => {
  return (
    <ConfigProvider>
      <div>home page</div>
    </ConfigProvider>
  )
}

export default HomePage
